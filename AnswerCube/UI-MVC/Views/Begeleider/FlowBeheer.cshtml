@model NoteModel

@{
    ViewBag.Title = "Flow Management";
    Layout = "_Layout";
}

<h2>Flow Management</h2>

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h3>Notes</h3>
            <form asp-controller="Begeleider" asp-action="AddNote" method="post" class="form-group">
                <input type="hidden" asp-for="InstallationId" value="" class="form-control"/>
                <div class="mb-3">
                    <label asp-for="Note" class="form-label"></label>
                    <textarea asp-for="Note" class="form-control" id="note" placeholder="Enter your note here"></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Add Note</button>
            </form>
        </div>
        <div class="col-md-6">
            <h3>Flow Control</h3>
            <button class="btn btn-success" id="startFlow">Start Flow</button>
            <button class="btn btn-danger" id="stopFlow">Stop Flow</button>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/flowManagement.js"></script>
}